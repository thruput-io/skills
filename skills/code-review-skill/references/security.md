# Security Rules

When reviewing code for security vulnerabilities, look for:
- Injection flaws (SQL injection, XSS, command injection).
- Hardcoded secrets, credentials, or API keys.
- Insecure direct object references (IDOR).
- Missing authorization or authentication checks.
- Cross-Site Request Forgery (CSRF).
- Improper error handling that leaks sensitive information.
